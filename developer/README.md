# Developer Tools

Signal Schedule developer-only pages remain physically stored under `/pages/...` and are exposed through clean `/developer/.../` routes by `_redirects`. Do not add public alias folders here unless a future build explicitly needs static assets that cannot be served from `/pages/`.
