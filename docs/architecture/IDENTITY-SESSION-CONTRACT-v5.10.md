# Identity Session Contract v5.10.0

Preview session shape:

- `user_id`
- `employee_id`
- `agency_id`
- `display_name`
- `email`
- `roles[]`
- `permissions[]`
- `setup_status`
- `landing_page`

The static v5.10 build uses `/data/current-user-preview.json` only. Live enforcement belongs to the backend/API layer.
